# 배포본 수정 내역 (2026-08-16)

업로드해 주신 배포본 `index.html` 을 검사해서 3건을 고쳤습니다.
**1번이 가장 중요합니다 — 파이프라인 작업이 사용자에게 도달하지 않고 있었습니다.**

---

## 1. 🔴 앱이 v2 좌표를 안 읽고 있었음

```js
// 이전 — provider 원본만 읽음
const nv = normScale(pr.valence), ne = normScale(pr.energy);
```

`v2_valence` 를 읽는 코드가 **파일 전체에 0곳**이었습니다.

즉 그동안의 V축 작업이 전부 앱에 반영되지 않았습니다:

- 골드셋 249곡 (쌍비교 964건 + DEAM 앵커 절대척도 정렬)
- 가사 결합 96곡 (L2 식)
- 수작업 태깅 32곡
- 골드셋 척도정렬 · 정밀도 가중 결합 · 계단 보정 · 퍼센타일 정규화

사용자는 계속 **provider 원본 valence** 로 추천받고 있었습니다. 한국 곡 상관 0.323짜리 값입니다.

인수인계 문서에 적혀 있던 *"앱이 데이터팀 스펙 대신 하드코딩을 써서, 축을 바꿔도 사용자에게
도달하지 않는다"* 는 우려가 실제로 벌어져 있던 상태였습니다.

**수정:** `v2_valence` / `v2_arousal` 우선, 없는 곡만 `provider_raw` 폴백.
콘솔과 상태 문구에 **v2 좌표 커버리지 %** 를 찍습니다 — 배포 후 이 숫자를 꼭 확인하세요.
100% 가 아니면 그만큼의 곡이 아직 provider 값으로 추천되고 있다는 뜻입니다.

`va_source` 와 `popularity` 도 엔진에 전달하도록 추가했습니다. 규칙의 신뢰도 동점 정렬에
실제 필드값(`golden_set_calibrated` 등)을 반영해 사람이 검증한 곡이 먼저 나옵니다.

## 2. 태그 6종 → 3종 전환

`calmness` · `*_suitability` · `lyric_distraction` · `instrumental_probability`
→ `vocal_load` · `groove` · `tonality`

곡 로딩에서 `v2_tags` 를 우선 읽고, 가사 선호 필터를 `vocal_load` 방향으로 반전했습니다
(vocal_load 는 높을수록 목소리 비중이 크므로 기존 `instrumental_probability` 와 반대 방향).

태그는 v2.1 엔진에서 **점수가 아니라 게이트로만** 쓰이므로 이 교체가 추천 순위를 직접 흔들지는
않습니다. 표시용 라벨과 가사 필터만 영향받습니다.

## 3. 조용한 실패 수정

엔진 로드가 실패하면 에러 메시지를 써놓고 화면 전환을 안 해서, **버튼을 눌러도 아무 일도
안 일어나는 것처럼** 보였습니다. 이제 화면이 넘어가고 실패 원인까지 표시합니다.

---

## 배포 시 확인할 것

```
[AZT] 엔진 로드 v2.1.0+3347a588511c
[AZT] V/A 소스 — v2 파이프라인 ____곡 / provider 폴백 ____곡 (___%)
```

브라우저 콘솔에서 이 두 줄을 확인하세요. 두 번째 줄의 비율이 이번 수정의 실제 효과입니다.

---

# 2차 수정 (같은 날, 후속 배포)

## 4. 🔴 R6 시간 감쇠가 구현돼 있는데 작동하지 않았음

`engine.js` 는 `0.5^(last_days / half_life_days)` 로 반감기를 정확히 계산합니다.
그런데 `index.html` 이 넘기는 `last_days` 가 **전부 `0`** 이었습니다:

```js
song_feedback[id] = { ...splitSignal(...), last_days: 0 };
for (const id of LIKED_SONGS)    song_feedback[id] = { pos: 3, neg: 0, last_days: 0 };
for (const id of PLAYLIST_SONGS) song_feedback[id] = ... || { pos: 2, neg: 0, last_days: 0 };
```

`0.5^(0/14) = 1` 이므로 **모든 피드백이 항상 "오늘 것"** 이었고, 반감기 14일이 사실상
무한대였습니다. 규칙 파일에는 있고 엔진도 구현했는데 앱이 입력을 안 채워서 죽어 있던
상태입니다. R6 를 "적용됨"으로 보고하면 안 되는 상태였습니다.

**수정:**
- `context_events` 의 `created_at` 으로 곡별 경과일 계산
- 엔진이 곡당 감쇠계수를 하나만 받으므로 **기여도(|v|) 가중 평균 경과일**을 넘김
  (최신 1건만 쓰면 오래된 누적이 안 늙고, 단순평균은 큰 신호를 과소평가)
- 좋아요·벽 붙이기는 `users` 문서에 시각이 없으므로 이벤트 로그에서 최근 시각을 회수
- `song_stats`(전체 집계)는 시각 필드 자체가 없어 감쇠 없이 유지 — 시간축이 필요하면
  `song_stats` 에 최근 갱신 시각을 추가해야 함

**검증** (합성 카탈로그 120곡, 30곡에 좋아요):

```
last_days=0   → 좋아한 곡이 시퀀스에 4/5곡
last_days=140 → 좋아한 곡이 시퀀스에 0/5곡
```

## 5. full / preview 구분 로깅 추가

Spotify iFrame 은 로그인 상태에 따라 전곡 또는 30초 미리듣기를 줍니다(모바일은 항상 30초).
같은 `track_complete` 가 preview 에서는 "30초 완주", full 에서는 "4분 완주"를 뜻하는데
구분 없이 쌓이고 있었습니다. **모바일=preview 가 100% 겹쳐 기기와 노출량이 완전히
교락되므로, 미니 실험에서 "알고리즘 효과"와 "노출 시간 효과"를 분리할 수 없었습니다.**

**판정은 추정이 아니라 관측:** embed 가 실제로 준 재생 길이를 카탈로그 원곡 길이와 비교합니다.
`duration <= 30000` 같은 절대값 비교는 30초 미만 짧은 곡을 오판하므로 쓰지 않습니다.

```
available_ms < catalog_ms * 0.9  →  preview
그 외                            →  full
카탈로그 길이 없음 / 재생 전      →  unknown
```

**추가된 필드**

| 위치 | 필드 |
|---|---|
| `context_events` 신규 이벤트 `playback_mode` | `playback_mode` · `available_ms` · `catalog_ms` |
| `track_complete` | `playback_mode` · `available_ms` · `catalog_ms` (기존 `completion_rate` 는 이미 비율) |
| `track_milestone` | `playback_mode` |
| `post_change` · `after_unlock` · `sequence_complete` | `playback_mode_session` (`full`/`preview`/`mixed`/`unknown`) |
| `users/{uid}/listeningHistory` | `playbackMode` · `catalogSeconds` |
| `song_stats` | `completes_full` · `completes_preview` · `sum_completion` · `n_completion` |

마지막 줄이 **R4(통계 비율화)** 의 입력입니다. preview 30초 완주와 full 4분 완주가
`completes` 로는 똑같이 +1 이라, 비율 없이는 두 모집단이 섞입니다.

## 6. 문서 숫자 정정 — V–A 상관

기존 "V–A 상관 −0.564" 는 실제 배포 카탈로그에서 재현되지 않습니다.
`data/processed/music_catalog_v2_20260816.csv` (2,398곡) 직접 계산:

| | 실측 |
|---|---|
| step δ=0.20 (현재 채택) | **r(V, A) = +0.486** |
| linear λ=0.5 (직전) | r(V, A) = +0.435 |

부호가 반대입니다. V 와 A 는 원래 **양의 상관**(config 의 λ=0 기록: +0.739)이고,
각성 보정은 그 양의 상관을 깎으려고 존재합니다. 발표 자료의 −0.564 / −0.027 은
`build_v2_catalog.py` 가 실행할 때 찍는 `r(V,A) 보정전 → 보정후` 출력으로 교체하세요.

---

# 3차 — "경로 왜이래" 후속 확인

## 7. 🔴 "걸음 상한을 시간에 따라 동적으로" 수정이 배포본에 없습니다

팀에서 *"MAX_STEP_JUMP 값이 너무 크게 고정돼 있어서 값이 튄다 → 시간에 따라 동적으로
조정하게 ISO 함수를 수정한다"* 고 한 건, **GitHub 어디에도 반영돼 있지 않습니다.**

- `engine/engine.js` 는 저장소에 처음 올라온 뒤(`3cb2373`) **한 번도 수정된 적이 없습니다.**
- 그 다음 커밋 `c49f8bd` 의 규칙 변경은 4줄이 전부이고, 내용은 `va_source` 동점정렬에
  `golden_set_calibrated` · `golden_set_provisional` 를 추가하고 `rules_hash` 를 갱신한 것입니다.
- 브랜치 3개(`main` · `feat/v2-valence-selected` · `fix/mobile-ui`)를 모두 확인했고
  뒤의 둘은 이미 `main` 에 병합돼 있습니다. **미반영 브랜치는 없습니다.**

로컬에서만 고치고 push 가 안 됐을 가능성이 큽니다. 해당 작업본을 올려 주세요.

### 현재 배포본의 시간 의존성

ISO 가 시간에 따라 움직이긴 하지만, **정확히 걸음 상한만 시간과 무관합니다**:

| 항목 | 시간 의존 | 근거 |
|---|---|---|
| 곡 수 `song_count` | ✅ | `songCount(rules, durationMin)` |
| 전환 시점 `transition_point` | ✅ | `transitionAt(rules, durationMin)` — 10분 0.7 / 30분 0.5 / 90분 0.45 |
| **걸음 상한 `max_step_jump`** | ❌ | 0.4 고정 · **스트레스**로만 변조 · **좌표계**로만 배율 |

`durationMin` 은 걸음 상한 계산에 전혀 들어가지 않습니다.

### 다만, 상한을 시간 의존으로 바꿔도 화면의 뭉침은 사라지지 않습니다

현재 걸음 상한은 규칙 파일에서 옵니다:

```
iso.max_step_jump 0.4  ×  stress_step_limit[stress=2] 0.4  ×  step_limit_scale.percentile 1.8
= 0.72          ← 퍼센타일 평면의 최대 거리는 1.414
```

실카탈로그 2,326곡으로 재현한 결과 **상한은 한 번도 걸리지 않습니다**:

| 시나리오 | 곡 간 실제 점프 | 상한완화 |
|---|---|---|
| 불안→편안 30분 (5곡) | 0.262 / 0.150 / 0.093 / 0.094 | 0걸음 |
| 불안→편안 60분 (10곡) | 0.028 ~ 0.190 | 0걸음 |
| 지침→활기 30분 (5곡) | 0.377 / 0.305 / 0.195 / 0.145 | 0걸음 |

상한 0.72 는 **너무 커서 튀게 만드는 게 아니라, 너무 커서 아무 역할도 안 합니다.**
점프 크기를 정하는 건 상한이 아니라 경유지 간격입니다. 그러니 상한을 시간 의존으로
바꾸는 수정은 방향은 맞지만(죽은 제약을 살리는 일), **화면에서 본 뭉침은 그걸로 안 없어집니다.**
그건 아래 8번이 원인입니다.

## 8. 🔴 진짜 원인 — `band` 붕괴로 기하 레인이 통째로 무력화돼 있었습니다 (수정 완료)

`preference.band = 0.16` 은 **합성 카탈로그 120곡에서 고른 값**입니다. 실카탈로그 2,326곡에서
재보니 밴드 하나에 **평균 203곡**이 "적합도 동급"으로 묶입니다. 카탈로그의 8.7% 입니다.

그러면 어떤 일이 벌어지냐면 — `chosen_by` 가 **100% `preference`** 가 됩니다.
즉 *"기하가 어디로 갈지 정하고, 선호가 그중 무엇을 고른다"* 는 두 레인 설계에서
**기하 레인이 아무것도 정하지 않게 됩니다.** 200곡 중 무엇을 고를지는 선호와 시드가 정하고,
그 200곡은 경유지에서 제각각 흩어져 있습니다. 그게 화면의 튐입니다.

측정 (시나리오 8개 × 시드 12개 = 480픽, 30분):

| band | 평균 밴드크기 | 최대점프 | **여정초과%** | 평균적합도오차 | 커버리지 | 첫곡최빈 |
|---|---|---|---|---|---|---|
| 0.03 | 6.5 | 0.520 | 0.0% | 0.0202 | 145곡 | 4% |
| **0.05 ← 채택** | **21.1** | **0.556** | **0.6%** | **0.0320** | **253곡** | **4%** |
| 0.08 | 53.2 | 0.662 | 2.5% | 0.0497 | 306곡 | 3% |
| 0.12 | 116.6 | 0.861 | 6.0% | 0.0747 | 330곡 | 4% |
| 0.16 ← 이전 | 203.2 | 0.861 | **9.2%** | 0.1007 | 309곡 | 4% |

**여정초과%** = 곡과 곡 사이 이동이 `지금→목표` 전체 거리보다 컸던 비율. "경로가 튄다" 의 직접 지표입니다.
이전 값에서는 **열에 하나가 목표까지 가는 거리보다 멀리 뛰고 있었습니다.**

경로 비교 (같은 입력·같은 시드, 원좌표):

```
지침→활기  (여정 0.781)
  0.16  지금(0.30,0.25) → (0.00,0.01) → (0.66,0.49) → (0.73,0.81) → (0.82,0.77) → (0.78,0.79)
        └ 첫 곡이 카탈로그 극단 구석. 2번째 점프 0.809 = 여정보다 김 ★
  0.05  지금(0.30,0.25) → (0.13,0.23) → (0.59,0.53) → (0.78,0.86) → (0.79,0.87) → (0.80,0.87)
        └ 최대 점프 0.552, 여정초과 없음

그냥→집중  (여정 0.071 — 거의 제자리)
  0.16  (0.43,0.54) → (0.59,0.43) → (0.51,0.43) → (0.58,0.41) → (0.57,0.43)   최대점프 0.193 = 여정의 2.7배 ★
  0.05  (0.50,0.52) → (0.52,0.46) → (0.56,0.48) → (0.55,0.46) → (0.55,0.45)   최대점프 0.065 = 여정 이내
```

**`band: 0.16 → 0.05` 적용.** 커버리지는 253곡으로 유지되고(0.03 으로 더 조이면 145곡으로 43% 감소),
밴드크기 21곡이라 선호·시드 변이가 여전히 작동합니다.

## 8-B. 부차 요인 — `transition_point` 로 경유지 절반이 목표에 겹칩니다

`waypoints()` 는 `t = min(1, k/(n-1)/at)` 로 경유지를 놓습니다. 30분이면 `at=0.5`, `n=5`:

```
경유지 1 (0.086, 0.643)  ← 지금
경유지 2 (0.367, 0.369)  ← 유일한 중간 지점
경유지 3 (0.647, 0.095)  ← 목표
경유지 4 (0.647, 0.095)  ← 목표 (동일)
경유지 5 (0.647, 0.095)  ← 목표 (동일)
```

**5곡 중 3곡이 완전히 같은 한 점을 향해 뽑힙니다.** 화면에서 목표 근처에 점이 뭉쳐 보이는
게 이것이고, 60분(10곡)에서는 5/10 이라 훨씬 자연스럽게 보입니다 — 팀이 올린 두 스크린샷이
서로 달라 보였던 이유입니다.

이건 **버그가 아니라 ISO 설계 의도**(목표 도달 후 그 상태를 유지)입니다. 다만 곡 수가
적을수록 "유지 구간" 비율이 커져 경로가 아니라 정체로 보입니다.

**⚠️ 짧은 세션은 ISO 가 아예 성립하지 않습니다.** `song_count.min = 3` 이고 `at=0.5` 면
`t = 0, 1, 1` — **중간 경유지가 0개**, 두 번째 곡이 곧바로 목표입니다.
`floor(분/6)` 이므로 **5~23분 요청은 전부 이 상태**입니다(슬라이더 최솟값이 5분).

8번(band)을 고치면 경로 자체는 정상화되지만, **목표 근처에 곡이 뭉치는 모양은 남습니다** —
그건 ISO 의 "유지 구간" 설계 의도이기 때문입니다. 짧은 세션에서 유지 구간 비율이 과한지는
음악중재 자문이 걸린 사안이라 **팀 결정으로 남겨둡니다.**

## 9. 🔴 규칙 소스·컴파일러·Python 검증기가 저장소에 없습니다

추천 알고리즘 문서의 핵심 주장은 *"규칙을 파일 한 장으로 빼고 JS·Python 두 엔진이 그걸
읽기만 한다 / 검사기가 v1 의 실수 5가지를 컴파일 오류로 막는다"* 입니다.
그런데 두 저장소를 전수 조사한 결과 **컴파일 산출물만 있고 나머지가 전부 없습니다**:

| | 상태 |
|---|---|
| `rules/rules.compiled.json` | ✅ 있음 (web) |
| `engine/engine.js` | ✅ 있음 (web) |
| `rules/v2.rules.yaml` (규칙 소스) | ❌ 없음 — `index.html` 주석이 참조하는데 실물 없음 |
| 규칙 컴파일러 + "이렇게 쓰면 안 됨" 검사기 | ❌ 없음 |
| `engine.py` (Python 검증기) | ❌ 없음 |
| `variety_check.py` · `ablate.py` | ❌ 없음 — 규칙 파일이 재측정 도구로 지목하는데 실물 없음 |

**결과로 막히던 것 / 지금 상태:**

- ~~규칙 값을 못 바꿈~~ → **해소.** `tools/rules_hash.mjs` 를 추가했습니다 (아래 12번).
  8번의 `band` 수정이 이걸로 가능해졌습니다.
- **`trace_keys` 에 없는 키를 못 늘립니다.** 10번의 좌표계 문제를 고치려면 키가 필요한데,
  허용 목록을 늘리는 게 규칙 설계 변경이라 소스 없이는 근거를 남길 수 없습니다.
- **검사기가 없어 v1 실수 5가지 차단이 실제로는 작동하지 않습니다.**
  문서가 "실제로 막았습니다" 라고 적은 부분이 현재 사실이 아닙니다.
- **JS/Python 패리티 검증이 불가능합니다.** 대조할 Python 엔진이 없습니다.
- **`variety_check.py` · `ablate.py` 가 없어** 재측정을 매번 임시 스크립트로 해야 합니다.
  8번 표는 그렇게 뽑았습니다 — 도구로 저장소에 넣어야 재현 가능해집니다.

## 10. trace 가 두 좌표계를 섞어 기록합니다

`engine.js` 가 남기는 trace 에서

- `wp_V` · `wp_A` — **퍼센타일** 좌표
- `song_V` · `song_A` — **원** V/A 값 (`Number(s.V)`)

두 쌍이 이름만 보면 비교 가능해 보이는데 좌표계가 다릅니다. 로그로 "곡이 경유지에서 얼마나
떨어졌나"를 계산하면 틀린 값이 나옵니다(실제 거리는 `va_distance` 에 따로 있음).
예: 경유지 (0.086, 0.643) · 곡 (0.365, 0.617) → 직접 계산하면 0.280, 실제는 **0.126**.

고치려면 trace 키 추가가 필요해 9번에 막힙니다.

## 11. 성능 — 영역 풀 중복 계산 제거 (동작 변화 없음)

같은 걸음의 빔 상태 8개는 경유지가 같아 영역 풀도 같은데, `stepCandidates` 가 상태마다
카탈로그 전수를 다시 걸렀습니다. `recommend()` 에서 걸음당 1회만 계산해 넘기도록 바꿨습니다.

**검증:** 실카탈로그 2,326곡 · 시나리오 24개(스트레스 0~4, 5~90분, 최근재생 제외 포함)에서
곡 순서·`va_distance`·`band_size`·`chosen_by`·`path_cost` **24/24 완전 일치**, 평균 144ms → 138ms.

## 12. `tools/rules_hash.mjs` 추가 — 규칙을 고칠 수 있게 됐습니다

`rules_hash` 는 모든 추천 로그(`recommendations.algorithm_version`)에 박히는 값이라, 규칙이
바뀌었는데 해시가 그대로면 "어떤 규칙으로 뽑힌 추천인지" 구분이 사라집니다. 컴파일러가
없어서 규칙을 손으로 고칠 수 없던 이유가 이것이었습니다.

```bash
node tools/rules_hash.mjs           # 검증만 (불일치면 exit 1 — CI 에 걸어두세요)
node tools/rules_hash.mjs --write   # 갱신
```

**해시 정의 (2026-08-16 이후):** `sha256(키 정렬 JSON, rules_hash 필드 제외)` 의 앞 12자리.
같은 내용이면 누가 언제 돌려도 같은 값이 나옵니다.
※ 이전 값 `76edd8a3013c` · `3347a588511c` 는 옛 컴파일러가 찍은 것이라 이 정의로 재현되지
않습니다. **정의가 바뀐 지점이 여기입니다.** 규칙 소스와 컴파일러가 복구되면 이 스크립트의
정의를 컴파일러에 맞추거나, 컴파일러를 이 정의에 맞추세요.

이번 배포의 해시: `3347a588511c` → **`f4c2d3ce1e4b`** (band 0.16 → 0.05)

---

# 4차 — `min_pool` · `step_limit_scale` · `radius` 재측정

전 구간 측정: 시나리오 8개 × 스트레스 0~4 × 길이 10~90분 × 시드 3 = **480회 / 조합**.

## 13. `min_pool = 12` 는 죽은 값이었습니다 → 48

12 / 24 / 48 이 **모든 지표에서 완전히 동일**했습니다. 반경 0.28 이 이미 충분히 크기 때문입니다:

| 지점 (퍼센타일) | 반경 0.28 | 0.15 | 0.08 |
|---|---|---|---|
| 중앙 (0.50, 0.50) | 633곡 | 172곡 | 47곡 |
| 활기 구석 (0.85, 0.85) | 558곡 | 278곡 | 88곡 |
| 침잠 구석 (0.15, 0.15) | 501곡 | 167곡 | 44곡 |
| 휴식 구석 (0.85, 0.15) | 245곡 | 107곡 | 30곡 |
| **격정 구석 (0.15, 0.85)** | **234곡** | **83곡** | 22곡 |

가장 희소한 지점도 234곡이라 **반경 성장 루프가 한 번도 발동하지 않았습니다.**
`48 = beam_width(8) × expand_per_step(6)` — 빔이 한 걸음에 실제로 소비하는 후보 수로 맞췄습니다.
지금도 발동하지 않지만, 반경을 줄이거나 카탈로그가 희소해지면 실제 가드로 작동합니다.

## 14. `radius = 0.28` → 0.15 — 품질 동일, **속도 3.7배**

`band=0.05` 가 어차피 가장 가까운 밴드만 고르므로, 633곡을 끌어오는 건 순수 낭비였습니다.

| radius | 적합도오차 | 커버리지 | 여정초과 | **ms/추천** |
|---|---|---|---|---|
| 0.28 (이전) | 0.0320 | 338곡 | 0.0% | **207** |
| 0.20 | 0.0318 | 336곡 | 0.0% | 105 |
| **0.15 ← 채택** | 0.0317 | 333곡 | 0.0% | **56** |
| 0.10 | 0.0314 | 331곡 | 0.0% | 26 |
| 0.06 | 0.0314 | 325곡 | 0.0% | 13 |

품질은 평평하고 속도만 16배 차이납니다. 0.15 를 고른 이유는 가장 희소한 격정 구석에서도
83곡을 잡아 빔 수요 48 을 넘기 때문입니다 — 더 줄이면 `min_pool` 가드에 자주 걸립니다.

## 15. `step_limit_scale.percentile = 1.8` → 1.3 — 상한이 일하기 시작합니다

상한 = `stress_step_limit[stress] × scale`.

| scale | 상한완화% (stress 0/1/2/3/4) | 적합도오차 | 커버리지 |
|---|---|---|---|
| 0.9 | 0.4 / 0.4 / 1.1 / 2.3 / 4.2 | 0.0350 | 419곡 |
| 1.1 | 0.0 / 0.3 / 0.4 / 1.1 / 2.5 | 0.0338 | 405곡 |
| **1.3 ← 채택** | 0.0 / 0.0 / 0.0 / 0.4 / 1.1 | 0.0334 | 384곡 |
| 1.4 | 0.0 / 0.0 / 0.0 / 0.4 / 1.1 | 0.0327 | 374곡 |
| 1.8 (이전) | 0.0 / 0.0 / 0.0 / 0.0 / 0.4 | 0.0318 | 345곡 |

이전 값 1.8 은 **"상한완화 0%" 만 보고 고른 값**인데, 그러면 상한 자체가 죽습니다.
stress=4 에서 상한이 0.45 (퍼센타일 평면 최대거리 1.414 의 32%) 라 실측에서 사실상 걸리지
않았습니다. 즉 *"스트레스가 높을수록 한 걸음을 작게"* 라는 설계 의도가 **꺼져 있었습니다.**

**⚠️ 완화율만 보면 거꾸로 읽게 됩니다.** 곡 사이 퍼센타일 이동이 **0.325 이내인 비율**로 재면:

| | 0.325 이내 | 완화 (전체 / stress4) |
|---|---|---|
| 이전 (0.28 / 12 / 1.8) | 93.1% | 0.1% / 0.4% |
| **채택 (0.15 / 48 / 1.3)** | **93.8%** | 1.2% / 3.4% |

**완화는 늘었지만 걸음은 실제로 더 조여집니다.** 완화가 늘어난 건 제약이 일을 하기 시작했다는
뜻이고, 완화된 걸음은 전부 `trace.step_relaxed` 로 기록되므로 사후 감사도 가능합니다.

## 16. 종합 — 이전 vs 채택

| | 이전 | 채택 | |
|---|---|---|---|
| 걸음이 0.325 이내 | 93.1% | **93.8%** | 개선 |
| 커버리지 | 345곡 | **372곡** | **+7.8%** |
| 고유 시퀀스 | 126 | **156** | **+24%** |
| 소요 시간 | 217ms | **63ms** | **3.5배** |
| 적합도오차 | 0.0318 | 0.0319 | 사실상 동일 |
| 여정초과 | 0.0% | 0.0% | 동일 |
| 상한완화 (stress4) | 0.4% | 3.4% | 제약이 작동 시작 |

`rules_hash`: `f4c2d3ce1e4b` → **`77ec06de1a02`**

---

# 5차 — 곡 수 재설계 (팀 요청)

## 17. `song_count` — 6분/곡 가정을 실측 3.73분으로, 상한 15 → 9

`per_minutes: 6` 은 근거 없는 사전값이었습니다. Spotify export **3,289곡 실측**:

| | 값 |
|---|---|
| 중앙값 | **224초 = 3.73분** |
| 평균 | 227초 = 3.77분 |
| 25%~75% | 3.29 ~ 4.20분 |

`per_minutes: 3.73` · `min: 3` · `max: 9` (max 15→9 은 팀 결정)

```
5분→3곡 · 10분→3곡 · 15분→4곡 · 20분→5곡 · 25분→6곡 · 30분→8곡 · 40분 이상→9곡
```

30분 → 8곡 × 3.73 = 29.8분으로 요청 시간과 실제 재생 시간이 맞습니다.
34분을 넘는 요청은 경유 시퀀스가 9곡에서 멈추고 나머지는 추가 추천으로 채웁니다.
5분 요청 처리는 **보류** 상태입니다 (min=3 이라 11분 분량이 나옵니다).

**앱의 하드코딩 제거:** `index.html` 이 `Math.ceil(분/6)`, `3~15` 를 따로 갖고 있었습니다.
규칙 파일(`iso.song_count`)에서 읽도록 바꿨습니다 — 앱과 엔진이 서로 다른 곡 수를
가정하던 상태였습니다.

## 18. `min_step_span` 신설 — 지금·목표가 가까우면 경유 곡을 줄입니다

여정이 짧은데 곡을 많이 끼우면 걸음이 `band`(0.05)보다 잘게 쪼개집니다. 그 폭 안의
곡들은 순위가 구분하지 못하므로 어느 곡이 뽑힐지는 선호와 시드가 정하고, 경로가
"지금에서 목표로 가는 선"이 아니라 **좁은 덩어리 안을 맴도는 잡음**이 됩니다.

```
경유 곡 수 = max(song_count.min, min(시간 기준, floor(여정거리 / min_step_span) + 1))
```

`min_step_span: 0.05` — `band` 와 같은 값입니다. 순위가 구분할 수 있는 최소 폭이 곧 band 이므로,
band 를 바꾸면 이 값도 같이 검토해야 합니다.

실측 (30·60분 × 시드 4, 원좌표):

| 시나리오 | 여정 | span=0 | **span=0.05** | span=0.03 |
|---|---|---|---|---|
| 매우 가까움 | 0.071 | 9곡 · 점프 0.051 | **3곡 · 점프 0.089** | 4곡 · 점프 0.074 |
| 가까움 | 0.085 | 9곡 · 점프 0.068 | **3곡 · 점프 0.139** | 4곡 · 점프 0.089 |
| 중간 | 0.495 | 9곡 · 0.250 | 9곡 · 0.250 | 9곡 · 0.250 |
| 멂 | 0.617 | 9곡 · 0.359 | 9곡 · 0.359 | 9곡 · 0.359 |
| 매우 멂 | 0.781 | 9곡 · 0.448 | 9곡 · 0.448 | 9곡 · 0.448 |

**⚠️ 정직하게 — 가까운 여정에서 곡을 줄이면 개별 점프는 오히려 커집니다** (0.051 → 0.089).
곡이 많으면 목표 근처에 다 몰려 있어 한 걸음이 작아지기 때문입니다. 줄이는 이유는 점프
크기가 아니라 *"9곡을 들으며 제자리를 맴도는 것"* 을 막기 위해서입니다.

점프가 커지는 직접 원인은 **n=3 이 `transition_point` 의 퇴화 구간**이라는 점입니다
(at=0.5 일 때 t = 0, 1, 1 — 중간 경유지가 0개라 두 번째 곡이 곧바로 목표).
`transition_point` 를 손보면 span=0.05 에서도 점프가 줄어듭니다. 그 전까지 점프를 더
중시한다면 **span=0.03(곡 수 4)** 이 절충안입니다.

로그에서 어느 쪽이 곡 수를 정했는지는 엔진 출력의 `song_count` 필드
(`by_time` / `by_journey` / `effective` / `journey`)로 구분됩니다.

## 19. '더 들을 곡' 정렬 — 카탈로그 순서 → 목표 근접순

경유 시퀀스가 짧아질 수 있게 되면서 그 뒤 곡들의 비중이 커졌는데, 기존에는 **카탈로그
순서 그대로** 채우고 있었습니다. 경유는 '이동', 그 뒤는 '도착한 상태 유지' 역할이므로
목표 좌표에 가까운 순으로 채웁니다.

## 20. 전 구간 회귀 (추천 960회 · 스트레스 0~4 × 5~90분 × 시드 3)

| | |
|---|---|
| 곡 수 범위 | 3 ~ 9 |
| 여정 때문에 줄어든 추천 | 90회 (9.4%) |
| 상한완화 | 1.9% |
| 적합도오차 | 0.0324 |
| 커버리지 | 293곡 |
| 고유 시퀀스 | 213 |
| 소요 시간 | **50ms/추천** |

`rules_hash`: `77ec06de1a02` → **`1195d6383baa`**

> ⚠️ 커밋 `ab929a2` 의 메시지에는 `d7092eb919d6` 으로 적혀 있는데 **오기입니다.** 그 값은
> 근거 문구를 더 쓰기 전 중간 계산값이고, 배포된 실제 값은 `1195d6383baa` 입니다.
> `node tools/rules_hash.mjs` 로 언제든 확인할 수 있습니다.

---

## 남은 것

- **계단 보정** — `CONFLICT_계단보정.md` 참고. **팀 결정 필요 — 이번 배포에 포함 안 됨.**
  경계 A∈[0.45,0.55] 373곡에서 위/아래 V 평균차가 step 은 −0.147, linear 는 +0.008.
  독립 37곡 검증은 provider 기준선 0.443 / step 0.399 / linear 0.373 로 **셋 다 구분 불가**
  (n=37, SE≈0.17). 사람 판단으로는 못 고르므로 경계 연속성·사분면 균형으로 고르는 것이 타당.
  linear 의 유일한 약점(β가 표본마다 흔들림)은 계수를 고정하면 사라짐.
- **모바일 전곡 재생** — 정책상 막힘 (로깅은 위 5번으로 해결)
- **좌표계** — 골드셋 906건으로는 판정 불가 (밝기 순서만 묻는 데이터라 퍼센타일에도 판정 동일).
  `post_change` 로그로 판정하는 것이 현실적
- ~~band=0.16~~ → **실카탈로그 재측정 완료, 0.05 채택** (8번)
- ~~`step_limit_scale` · `min_pool`~~ → **실카탈로그 재측정 완료** (13~16번).
  `min_pool` 12→48, `radius` 0.28→0.15, `step_limit_scale` 1.8→1.3
- **`transition_point`** — 목표 근처 뭉침(8-B)과, **n=3 퇴화 구간**(18번). 후자는 이제
  `min_step_span` 때문에 가까운 여정에서 자주 발생하므로 우선순위가 올라갔습니다.
  `at=0.5` 에서 n=3 이면 t = 0, 1, 1 이라 중간 경유지가 0개입니다. 음악중재 자문 사안.
- **5분 요청** — `song_count.min=3` 이라 11분 분량이 나옵니다. 슬라이더 최솟값을 올릴지
  `min` 을 내릴지 제품 결정 필요 (팀 요청으로 이번엔 보류).

---

# 6차 (2026-09-17) — 문서에는 "수정 완료"인데 코드에 없던 것 복구 + 버그 2건

저장소 전수 점검 결과, 위 2~5차에 적힌 수정 일부가 **코드에 반영돼 있지 않았습니다**
(`last_days` 전부 0, `min_step_span` 엔진 미구현, 앱의 `분÷6` 곡 수, '더 들을 곡' 카탈로그 순서, 영역 풀 중복 계산).
이번에 실제로 넣었습니다. 새 숫자는 하나도 만들지 않았습니다 — 기존 규칙·문서의 의도를 코드에 맞춘 것뿐입니다.

| # | 파일 | 내용 | 추천 결과 영향 |
|---|---|---|---|
| 21 | index.html | 🔴 **`splitSignal` 부호 버그.** 긍정 비율을 `v` 로 계산해 +0.5 미만의 긍정 신호가 전부 부정으로 읽힘 — 완청 1회가 선호 0.50→**0.45**, Spotify 클릭이 **0.41**. `(1+v)/2` 로 수정 → 0.537 / 0.519 | 있음 (의도대로) |
| 22 | index.html | R6 시간 감쇠 입력 복구 — 기여도 가중 평균 경과일, 좋아요·벽 붙이기 시각은 이벤트 로그에서 회수 (4번의 설계 그대로) | 오래된 피드백만 |
| 23 | engine.js | `iso.min_step_span` 구현 (18번의 식 그대로, 여정거리는 band 와 같은 작업 좌표계). 출력에 `song_count{by_time, by_journey, effective, journey}` | 가까운 여정만 |
| 24 | index.html | 곡 수를 규칙 파일에서 읽음 (`분÷6, 3~15` 하드코딩 제거 — 30분에 앱 5곡/엔진 8곡으로 어긋나 있었음) | 장르 풀 판정만 |
| 25 | index.html | '더 들을 곡' 목표 근접순 + 최근 재생 제외 (19번) | 추가 곡만 |
| 26 | rules + index.html | `transition_point` 임시 오버라이드를 규칙 파일로 이관, 앱의 덮어쓰기 삭제 | **없음** |
| 27 | engine.js | 영역 풀 걸음당 1회 계산 (11번) | **없음** |
| 28 | engine.js + index.html | `ENGINE_VERSION`(2.2.0) 신설 → `algorithm_version` = `v2.1.0+<hash>+e2.2.0`. 규칙은 그대로인데 엔진이 바뀐 경우를 로그에서 구분 | — |
| 29 | sw.js | 🔴 셸 목록의 `/rules.compiled.json` 은 없는 경로 → `addAll` 전체가 조용히 실패해 **사전 캐시가 0개**였음. 경로 수정 + 엔진·규칙은 network-first (배포 직후 '새 화면 + 옛 규칙' 조합 방지). `azt-v6` → `azt-v7` | — |

**검증** (합성 카탈로그 2,398곡 · 시나리오 6 × 스트레스 3 × 길이 4 × 시드 2 = 144회)
- 27번 적용 전후 **144/144 동일**, 26번 적용 전후 **144/144 동일**
- 23번: 114/144 동일. 바뀐 30회는 전부 가까운 여정 두 시나리오(퍼센타일 여정 0.13 → 3곡, 0.26 → 6곡). 먼 여정 4개 시나리오는 전부 그대로
- `index.html` 메인 스크립트 · `sw.js` · `engine.js` `node --check` 통과

`rules_hash`: `1195d6383baa` → **`6a0c649526ef`**

## 아직 안 고친 것 (팀 결정 필요)

- **좋아요가 다음 추천을 바꾸지 못함** — 8곡 전부 좋아요를 눌러도 다음 세션 8곡 중 0곡이 달라짐(실행 확인).
  좋아요는 그 곡 하나만 올리는데, 그 곡은 최근 재생 60곡 제외에 걸려 후보에서 빠짐. 가수·장르로 일반화되지 않음.
- **동점 정렬(`va_source_rank` · `popularity`)은 죽은 규칙** — 정렬 키에서 시드 변이(jitter, 연속값) 뒤에 있어 도달하지 않음.
- 좋아요·싫어요는 로그인 사용자만 가능 / 정성 피드백(감정 칩·한 줄 기록·안 맞은 이유) UI 없음 / 날씨·시간 코드 없음
- `playback_mode` 로깅(5번) 미구현 — 카탈로그에 곡 길이(`duration_ms`)가 있어야 판정 가능
- 장르 데이터(`genres`) 부재

---

# 7차 (2026-09-17) — 정성 피드백 수집

목적: "왜 안 맞았는지"를 알고리즘 부품 단위로 모은다. 이전까지 정성 신호는 청취 후 −2~+2 슬라이더 하나뿐이라
어느 부품을 고칠지 정할 근거가 쌓이지 않았다.

**이유 칩 5종** (`index.html` 의 `MISFIT_REASONS` — 코드는 로그에 저장되므로 바꾸지 말 것, 문구는 바꿔도 됨)

| 코드 | 화면 문구 | 가리키는 부품 |
|---|---|---|
| `path_jump` | 분위기가 갑자기 튀어요 | 경로 설계 (경유지·band) |
| `not_my_taste` | 제 취향이 아니에요 | 선호 lane (개인화) |
| `mood_mismatch` | 지금 제 기분이랑 달라요 | 입력 해석 (감정 칩 좌표·Gemini) |
| `vocal_bother` | 가사·목소리가 거슬려요 | 가사 게이트 |
| `too_repetitive` | 너무 자주 나와요 | 다양성 (최근 재생 제외) |

**받는 곳 두 군데**

| | 세션 단위 | 곡 단위 |
|---|---|---|
| 위치 | '듣고 난 뒤' 카드, 슬라이더 아래 | 싫어요를 누른 직후 그 카드 아래 |
| 형태 | 칩 복수 선택 + 한 줄 주관식(200자) | 주된 이유 하나 (고르면 닫힘, 안 골라도 싫어요는 유효) |
| 대상 | 익명 포함 전원 (기존 60초 청취 게이트 그대로) | 로그인 사용자 (싫어요가 회원 전용 — 9/17 결정 유지) |
| 저장 | `post_change.payload.misfit_reasons[]` · `.note` | 새 이벤트 `dislike_reason {song_id, reason, position, role}` |
| 쓰임 | **집계 전용** — 개인 취향 점수에 넣지 않음 | 개인화 재료 (사용 방식은 아직 미정) |

**주관식은 숫자가 아니라 이유 코드로 분류한다** — `tools/classify_notes.mjs`.
"−0.6" 같은 점수는 왜 그 값인지 설명할 수 없지만, 분류는 근거가 "N명 중 M명"이라는 횟수가 된다.
앱은 원문만 저장하고 분류는 나중에 일괄로 돌린다(앱에 Gemini 호출 추가 없음, 기준을 바꾸면 원문으로 재분류 가능,
`api/gemini.js` 의 허용 action 2개 제한도 그대로). 구글 폼 서술형 응답 CSV 도 같은 도구로 같은 표에 합산된다.
분류의 신뢰도는 `--sample 30` → 사람이 먼저 분류 → `--validate` 로 "30개 중 몇 개 일치"를 낸다.

`tools/export_feedback.mjs` — Firestore 에서 위 기록을 내려받아 이유별 건수·사람 수를 집계한다(서비스 계정 필요).

**검증**: jsdom 으로 실제 클릭 재현 — 게이트 잠금, 칩 켬/끔, payload 내용, 제출 후 잠김, 이유 줄 중복 방지,
경로 곡/추가 곡 구분. 분류 도구 `--selftest`(허용 안 된 코드 → none, 응답 누락 → error, CSV 따옴표·줄바꿈) 통과.
집계 함수 단위 검증 통과. **미검증**: 실제 Gemini 호출, 실제 Firestore 읽기·쓰기 — 샌드박스에서 접근 불가.

⚠️ 배포 후 확인: Firestore 보안규칙이 `context_events` 의 type 을 허용 목록으로 제한하고 있다면 `dislike_reason` 이
거부된다. 싫어요 → 이유를 한 번 눌러 보고 콘솔에 "이벤트 로그 저장 실패"가 뜨는지 볼 것.

---

# 8차 (2026-09-17) — 개인화 재배선: "들어본 곡 중 좋아요한 비율"

**문제** (실행 확인): 한 세션 8곡 전부에 좋아요를 눌러도 다음 세션이 **0곡** 바뀜.
좋아요는 그 곡 하나만 올리는데 그 곡은 최근 재생 60곡 제외에 걸려 후보에서 빠지고, 가수·특징으로 넓혀지지 않았다.
온보딩에서 받은 선호곡(`favorite_tracks`)은 저장만 되고 추천에 쓰이지 않고 있었다.

**새 방식** — `preference.combine = "mean_like_rate"` (엔진 2.3.0)

```
내 평균        p0 = (좋아요한 곡 + 1) / (들어본 곡 + 2)
묶음 점수         = (그 묶음에서 좋아요한 곡 + 2·p0) / (그 묶음에서 들어본 곡 + 2)     ← 기록 없으면 p0
곡의 선호 점수    = 이 곡이 속한 묶음들(이 곡 · 가수 · 장르 · 말 비중 · 빠르기 · 연주곡 여부)의 단순 평균
```

- **들어본 곡** = 10초 이상 들은 곡 (`HEARD_MIN_SECONDS` — 좋아요 버튼이 열리는 조건과 같은 기준)
- **묶음**: 말 비중·빠르기는 전체 카탈로그 3분위(경계는 카탈로그에서 계산), 연주곡 여부는 참/거짓.
  장르는 카탈로그에 데이터가 채워지면 코드 수정 없이 합류한다.
- **우리가 정한 가중치는 없다.** 숫자 2 는 라플라스 계승 규칙의 가상 관측 2건. 나머지는 전부 그 사용자의 기록.
- **싫어요는 이유가 범위를 정한다**: 취향·이유 없음 → 모든 묶음 / 가사·목소리 → 말 비중·연주곡 여부만 /
  경로·기분·반복 → 어디에도 세지 않음(그 곡만 제외).
- 전체 사용자 통계는 들어본 곡이 하나도 없을 때(콜드스타트)만. 익명 사용자는 개인화 없음(9/17 결정).
- 화면: 내 기록 기준으로 한 버킷 이상 높은 곡에 "내가 좋아요한 곡들과 닮았어요 (말 비중·빠르기)" 설명.
- 로그: `recommendations.input.personalization {mode, heard, liked, like_rate}` · 곡별 `pref_score / pref_match / pref_basis`.

**첫 구현을 폐기한 기록** — 처음엔 좋아요 +1 / 싫어요 −1 '표'를 세고 기록 있는 항목만 평균했다.
검증에서 좋아요만 누르는 사용자에게 거의 모든 곡(세션당 7.9/8곡)이 '내 취향'으로 판정됐다. 카탈로그의 90% 가 보컬곡이면
좋아요의 90% 도 보컬곡이라 "보컬곡을 좋아한다"가 되는 **기저율 문제**. 들어본 곡 수로 나누는 방식으로 바꿔 해결.

**검증** (합성 카탈로그 2,398곡 · 가상 사용자 5유형 × 14세션 · 좋아하는 유형 = 말 비중 낮은 곡, 무작위 기대 33%)

| 가상 사용자 | 좋아하는 유형 (이전 → 새) | 싫어하는 유형 | 경로 적합도 오차 |
|---|---|---|---|
| 좋아요 + 싫어요(취향) | 35% → **77%** | 32% → 10% | 0.0351 → 0.0355 |
| 좋아요만 누름 | 35% → **77%** | 32% → 10% | 0.0351 → 0.0355 |
| 가끔만 좋아요(30%) | 35% → **63%** | 32% → 20% | 0.0351 → 0.0362 |
| 아무것도 안 누름 | 35% → 35% | 32% → 32% | 0.0348 → 0.0348 (완전 동일) |
| 싫어요를 '분위기가 튀어요'로만 | 35% → 33% | 32% → 35% | 0.0349 → 0.0349 |

- 이전 방식은 다섯 유형 모두 같은 결과 = 피드백을 전혀 반영하지 못했다는 뜻.
- **경로 적합도는 그대로** — 선호는 여전히 '적합도 동급(band)' 안에서만 순서를 바꾼다. ISO 경로는 건드리지 않는다.
- 기록 없는 사용자: 8차 전후 **144/144 동일**. 앱의 프로필 구성 코드 ↔ 엔진 통합 테스트 통과.
- ⚠️ 합성 카탈로그·가상 사용자다. 실제 효과는 로그인 사용자의 기록이 쌓인 뒤 5단계(replay)에서 잰다.

`rules_hash`: `6a0c649526ef` → **`7a9dc8823c84`** · `algorithm_version` = `v2.1.0+7a9dc8823c84+e2.3.0` · 서비스워커 `azt-v8`

**아직 남은 임의값** (2단계 대상): 반감기 14일, `global_k` 10, `pref_bucket` 0.05, 전체 통계 합성 계수(0.8/0.02/0.04/0.05),
`deriveStress` 0.6/0.4, 감정 칩 좌표 17개. 이전 방식 전용 값(`weights`, 좋아요=3회분, 완청 +0.3 등)은 새 방식에서 쓰이지 않는다.

---

# 9차 (2026-09-17) — 장르 선택이 추천에 반영되지 않던 문제

**원인** (Firestore 곡 문서를 직접 보고 확인)
1. 곡 데이터의 장르는 `genre` 필드에 **배열**로 들어 있다 — `genre: ["K-발라드"]`. 앱은 `genres`(배열) 또는 `genre`(문자열)만 읽어서
   2,398곡 전부 장르가 빈 값이 됐다. 9/16 콘솔 진단에서 "장르 0건"으로 나온 것도 `genres` 라는 이름만 봤기 때문이다.
2. 장르 곡이 0곡이니 "고른 장르의 곡이 부족하면 전체에서 추천" 규칙이 **매번, 아무 안내 없이** 발동했다 → 설문의 "K-pop 을 골랐는데 J-pop 이 나옴".
3. 데이터 표기("K-발라드")와 화면 버튼("K-pop", "발라드")은 이름이 다르고, 비교는 부분 문자열이어서 "팝"이 "K-팝"·"J-팝"·"시티팝"에 전부 걸리는 구조였다.

**수정** (`index.html`)
- `readRawGenres` — `genre`/`genres`, 배열/문자열 모두 읽는다. 원본 표기는 `genresRaw` 로 보존.
- `GENRE_RULES` — 곡을 불러올 때 데이터 표기 → 화면 장르 이름으로 변환. `K-` 로 시작하는 표기는 K-pop 에도 속한다("K-발라드" → K-pop, 발라드).
  이후 필터·엔진·개인화(장르 묶음)는 모두 화면 장르 이름을 쓴다 → 개인화의 장르 묶음이 자동으로 켜진다.
- 추천 시 비교는 이름이 **정확히 같은지**. 여러 장르를 고르면 그중 하나라도 해당하는 곡.
- 장르 버튼에 곡 수 표시, 0곡인 장르는 비활성.
- 고른 장르의 곡이 필요한 곡 수보다 적어 적용하지 못하면 결과 화면에 그 사실을 알린다(조용히 넘어가지 않음). 적용됐으면 "○○ 장르에서만 골랐어요".
- 로그: `recommendations.input.filters {lyric, genres, genre_applied, genre_pool, pool}`.
- 진단: 콘솔에서 `AZT_DEBUG.catalogReport()` — 원본 표기별 곡 수, 버튼별 곡 수, 안 잡힌 표기, 가사 조건별 곡 수.

**가사 조건** — 코드는 정상(9/17 태그 이름 수정 이후). `v2_tags.instrumental` 로 연주곡을 판정한다.
다만 "가사 적은 곡이 좋아요"는 `spokenness`(Spotify speechiness 의 순위 = **랩·말하듯 하는 정도**)로 거르므로
가사가 꽉 찬 발라드도 통과한다(예: Darin – Stood, spokenness 0.028). 가사의 '양'을 나타내는 데이터는 없다 → 문구 변경 검토.

**미확정**: `GENRE_RULES` 는 실제 표기 목록을 다 보기 전에 만든 것. `catalogReport()` 결과로 보완 필요.
